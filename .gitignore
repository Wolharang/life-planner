# macOS
.DS_Store
**/.DS_Store

# Local tooling state (Claude Code)
.claude/

# Node (defensive — app/ has its own .gitignore for app/node_modules)
node_modules/

# Logs
*.log
npm-debug.*

# Native projects are regenerated by `expo prebuild` (see app/.gitignore: /android, /ios)

# Gradle build artifacts (incl. the local native module app/modules/lp-alarm/android/)
**/.gradle/
**/build/
*.keystore
local.properties
