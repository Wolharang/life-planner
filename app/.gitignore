# dependencies
node_modules/

# expo
.expo/
dist/
web-build/
expo-env.d.ts

# native projects (regenerated by `expo prebuild`; the alarm module ships via a config plugin)
/android
/ios

# Firebase (F0) — per-project config and credentials. NEVER commit these.
google-services.json
GoogleService-Info.plist
.env
.env.*
firebase-debug.log
.firebase/

# misc
*.log
.DS_Store
*.orig.*
npm-debug.*
