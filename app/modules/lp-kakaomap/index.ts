// Native-only local module (autolinked via expo-module.config.json). The app talks to the native view through
// app/src/core/geo/KakaoMap.tsx (requireNativeViewManager('LpKakaoMap')), keeping the map behind /core/geo.
export {};
