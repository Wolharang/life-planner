// Native side is autolinked by Expo (see expo-module.config.json). The app talks to it
// through app/src/core/notifications/alarm.ts (requireNativeModule('LpAlarm')), which keeps
// the feature code behind the /core/notifications interface (architecture §5).
export {};
