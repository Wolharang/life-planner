{
  "name": "lp-holidays-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: pull Korean holidays from Kakao into KV and serve them to devices (D89).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.84.0"
  }
}
