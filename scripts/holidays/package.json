{
  "name": "lp-holidays-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GitHub Actions job: pull Korean holidays from the Kakao API and publish them to Firestore (D89).",
  "scripts": {
    "sync": "node index.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  }
}
